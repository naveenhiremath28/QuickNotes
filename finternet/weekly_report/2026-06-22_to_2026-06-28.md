# Weekly Report — 2026-06-22 → 2026-06-28

**Week:** Mon 2026-06-22 → Sun 2026-06-28 (2026-W26)
**Author:** Naveen
**Commits:** 16 across 3 repos (units-api, finternet-app, units-services)

## Summary

- Built end-to-end outer-envelope signature verification (units-api, ticket #235): a global, feature-flagged ed25519 middleware that resolves the public key from `key_references` and verifies the request's `{key_id, jws}` signature over the canonical payload, backed by a pluggable per-key-type verifier registry that no-ops when the flag is off or no signature is present.
- Made the signing and verification sides agree on exactly what bytes get signed — `/account/sign` now signs `JCS(payload)` alone (dropping the unreproducible ULIP context, txn_id, and sent_at) and returns the stable `key_references` UUID as the top-level `key_id`, so the middleware can reproduce and verify the signature against the raw request payload.
- Fixed a federation/ULIP regression where peer callbacks (e.g. `/v1/internal/primitives/complete`) carrying a self-describing peer signature with no JWS were wrongly routed into UUID key-reference parsing and 401'd — outer verification is now skipped whenever there is no JWS, a root-cause fix covering all peer routes, with a regression test added.
- Restored federation routing in the BFF (finternet-app): added the missing `tokenIssuerTarget` middleware to the `/clients/*`, `/scopes/*`, scope-approvals, and `workflows/execute` allowlist entries so cross-instance users' requests reach their JWT issuer's home units-api instead of 401'ing at the wrong default backend.
- Migrated the federation transfer path off the removed `/token/transfer` endpoint onto `/token/transact` with `operation: transfer` (#328), and removed the now-dead Lock-envelope / `federationTransfer` plumbing.
- Stood up a Postman/newman test harness for the signing flows (#235): a `postman-test` Makefile target, collection README, refreshed local/dev environments (federation topology, signing vars), and a fully populated Signed Transact folder with auto-sign pre-request scripts across all 13 operations.

Trailing fixes: reverted the never-required mandatory signature on token mint and simplified the `/account/sign` example bodies; fixed an auto-sign Postman bug where `{{placeholders}}` were signed before resolution (causing 401s); inlined security headers into `next.config.ts` to resolve a production module-resolution issue; and refreshed the units-services `docker-compose` and `.env.example`.

## By service

### units-api
- #235 signature feature: ed25519 outer-envelope verification middleware (feature-flagged, pluggable registry); `KeyReference` FindByID/FindByKeyName lookups; canonical-payload-only signing in `/account/sign`; sign response returns the `key_references` UUID + key_name; mandatory signature on transact.
- Peer-callback fix: skip outer verification when no JWS is present, unblocking federation/ULIP peer routes; regression test added.
- Postman/newman harness (#235, #235): newman target, README, environment refresh, Signed Transact folder, and auto-sign placeholder-resolution fix; mint signature revert and example-body cleanup.
- Updated transaction operation naming to use parsed plan data instead of the v2 field.

### finternet-app
- Routed `/clients/*`, `/scopes/*`, scope-approvals, and `workflows/execute` through `tokenIssuerTarget` so cross-instance traffic hits the home instance.
- #328: federation transfers now go through `/token/transact`; removed dead Lock-envelope plumbing.
- Inlined security headers into `next.config.ts` to fix production module resolution.

### units-services
- Refreshed `docker-compose` and `.env.example` for local dev.

---
*Generated from git commits by /weekly-report.*
