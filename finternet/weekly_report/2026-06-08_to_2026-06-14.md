# Weekly Report — 2026-06-08 → 2026-06-14

**Week:** Mon 2026-06-08 → Sun 2026-06-14 (2026-W24)
**Author:** Naveen
**Commits:** 12 across 3 repos (units-services, units-api, helmcharts)

## Summary

- Simplified the proof lifecycle data model across three services: dropped the redundant `transactions.proof_status` column and made `proof_id` itself the proven marker (NULL = pending, non-NULL = proven), with the per-batch anchoring lifecycle still tracked on `proofs.status`. Touched the proof service poll query, the units-api DDL (replacing the old index with a partial `idx_transactions_pending_proof` tuned for the poll), and the helmcharts Flyway migration that was reverted to keep the column out of fresh environments.
- Eliminated a split-brain risk in the proof service by stripping its duplicate `/v1/transaction/proof*` API and leaving only a health listener — the public proof API is now served solely by units-api, so two services no longer read the same `proofs` rows. Also tightened the batching poll to only pick up `completed` transactions.
- Hardened the proof service container by migrating it onto Docker Hardened Images (alpine3.23 builder + shell-less runtime), switching sqlx from native-tls to rustls to drop the OpenSSL C dependency and build a fully static musl binary, then pinning the real DHI rust tag (1.96-alpine3.23) and correcting the nonroot uid to 65532.
- Stood up the registry service's CI/CD from scratch — a lint/test workflow (build, golangci-lint, race tests with coverage, govulncheck) and a multi-arch (amd64/arm64) build-and-push pipeline — then fixed the missing DHI login step, resolved govulncheck advisories (Go 1.25.11, Fiber v2.52.12), and satisfied staticcheck in the unit tests.
- Cleaned up CI across all units-services workflows by removing the redundant push-to-main trigger so builds run on pull requests only, and added a `lifecycle_state` field to the registry's `CapabilityDocument`.

## By service

### proof service / units-services
- Made `proof_id` the proven marker and removed the dead `proof_status` column, helper, and model field; poll now filters on `proof_id IS NULL AND status = 'completed'`.
- Removed the duplicate proof API, leaving a health-only listener (public API consolidated into units-api).
- Migrated to DHI hardened base images with a static rustls/musl build; pinned the real rust tag and fixed the nonroot uid.

### registry / units-services
- Added lint/test and multi-arch build-and-push workflows; fixed the DHI login step; resolved govulncheck and staticcheck findings; added `lifecycle_state` to `CapabilityDocument`.
- Removed the redundant push-to-main CI trigger across all units-services workflows.

### units-api
- Dropped `transactions.proof_status` from the schema and added the partial `idx_transactions_pending_proof` index tuned for the proof service poll; removed the now-redundant prior index.

### helmcharts
- Reverted the `V1_20` proof_status Flyway migration (and its changelog/version bump) so the unused column stays out of main and fresh environments.

---
*Generated from git commits by /weekly-report.*
