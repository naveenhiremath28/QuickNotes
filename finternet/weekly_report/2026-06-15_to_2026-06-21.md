# Weekly Report — 2026-06-15 → 2026-06-21

**Week:** Mon 2026-06-15 → Sun 2026-06-21 (2026-W25)
**Author:** Naveen
**Commits:** 54 across 5 repos (units-api, finternet-app, stubs, units-services, units-token-runtime)

## Summary

- Delivered the OTP-based transfer authorization fallback (epic #230) for users without an MPC key: built a two-phase `/account/sign` flow in units-api that decrypts user PII (mobile-first, then email) to resolve the username and returns a signed lock envelope, wired the matching fallback UI into finternet-app's transfer page, and surfaced the OTP JWT in the auth response (units-services).
- Hardened the signing model in finternet-app by splitting *signing* from *submission* — the OTP modal now only verifies and signs, transfer is a separate explicit action, and the payload-bound signature is invalidated whenever the form changes, closing a security gap where a stale signature could authorize altered transfer details.
- Renamed the transfer amount field to `value` end-to-end across units-api (request structs, service layer, Go→Kafka payload builder, JSON schemas, golden vectors, and unit/e2e tests), and kept consumers in lockstep — the token engine's golden ULIP envelope/tamper tests (units-token-runtime) and the federation test harness (stubs) were realigned to the value-keyed payload.
- Stabilized the outer API signature envelope (units-api) through its final shape `{key_id, jws}`, applying the rename across all 24 request schema files plus the `common.Signature` Go struct and tests after an intermediate `{key_id, signature}` step was reverted.
- Added contact-based registry resolution (units-api): a new `ResolveByContact` path POSTing to the registry's `/v1/resolve`, threaded through the REST resolver and a cache that keys on a SHA-256 hash of the contact (never plaintext), enabling federation login forwarding — a foreign-home login now resolves the user by contact and forwards (307) to their home instance instead of failing.
- Fixed federation transaction visibility and identity (units-api): transaction initiators can now read their own federation transactions (matched by account ID, not just address), and transaction identities/participants are aligned on address+name with the recipient address hashed and `operationName` exposed.
- Corrected portfolio and transaction displays in finternet-app: tokens list, token details, and portfolio now show the holder's actual balance instead of total supply, and the transactions page sources from token transactions for units with operation resolved via an `operationName` fallback (#327).
- Switched the finternet-app transfer flow to asynchronous submission (#326) with updated registry configuration and an inline OTP sign flow showing the masked contact and a resend option.
- Made the GORM SQL log level configurable (units-api) via `DB_CONFIG.logLevel`, with a local `DATABASE_URL` default and `.env.example`/compose knobs, and expanded the base Postman collection with the federation, ULIP, sign, and OTP endpoints.

## By service

### units-api
- Renamed transfer `amount`→`value` across request structs, service layer, Kafka payload builder, 24 schemas, golden vectors, and tests.
- Settled the outer signature envelope on `{key_id, jws}` across all schemas, the `common.Signature` struct, and tests.
- Added `ResolveByContact` (registry client `/v1/resolve`, REST + SHA-256-hashed cached resolvers) powering 307 federation login forwarding.
- Fixed federation tx read access for initiators and aligned tx identities on address+name (hashed recipient, exposed `operationName`).
- Added the two-phase `/account/sign` OTP service + endpoint; made GORM SQL log level configurable; expanded the base Postman collection.

### finternet-app
- Built the no-MPC-key OTP transfer fallback (account/sign → signed lock envelope, PII-decrypted username, masked contact, resend).
- Split signing from submission and invalidated payload-bound signatures on form change; OTP modal renamed to "Verify".
- Switched transfer to asynchronous submission (#326) and fixed holder-balance display in portfolio, tokens list, and token details.
- Sourced the transactions page from token transactions with an `operationName` fallback (#327).

### units-services
- Surfaced the OTP JWT in the auth response to support the transfer authorization fallback.

### units-token-runtime
- Aligned the engine's golden ULIP envelope and tamper test to the value-keyed payload.

### stubs (federation test harness)
- Emitted `value` (not `amount`) in transfer and signed-lock payloads, added a signed-lock mismatch case mutating the value key, and added a `DB_CONFIG.logLevel` knob to the federation composes.

---
*Generated from git commits by /weekly-report.*
