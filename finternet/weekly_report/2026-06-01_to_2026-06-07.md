# Weekly Report — 2026-06-01 → 2026-06-07

**Week:** Mon 2026-06-01 → Sun 2026-06-07 (2026-W23)
**Author:** Naveen
**Commits:** 17 across 3 repos (helmcharts, units-automation, wpbe-silencelabs)

## Summary

- Built the **proof service** Helm chart from scratch — scaffolded it on the `finternet-base` dependency and authored the full template set (deployment, configmap, service, serviceaccount, ingress, and helpers) with a 165-line default values file matched to the proof-service runtime config, plus the `V1_20` `proof_status` Flyway migration for the service's polling path, secret/TLS override docs, and an initial-release changelog.
- Registered the proof service into the **GitOps automation (units-automation)** — added its ArgoCD application at dependency wave 3, created the dev-environment namespace, wired the composite `database_url` secret in GSM, and extended the network policy so the service rolls out cleanly behind the existing waves.
- Migrated **WPBE (wpbe-silencelabs)** CI and Docker build off the decommissioned Shipyard.rs registry onto the new Kellnr sparse-HTTPS index — dropped all SSH-agent/keyscan/cargo-login machinery and switched to a token-based pull, unblocking a pipeline that was failing at `ssh-keyscan`.
- Fixed two release-build breakages in WPBE surfaced by the `2026.06.03_RC` tag: restored `git` in the Docker builder stage (the `wallet-provider-service` `build.rs` shells out to `git describe` at compile time, which PR CI never exercised), and made image-tag handling consistent for RC tags — reworked to match the units-api / units-token-runtime pattern (tag kept verbatim, only the registry base lowercased).
- Aligned WPBE CI with the org workflow standard — added a `cargo audit` Security Audit job (with the RUSTSEC-2023-0071 rsa advisory documented as an accepted ignore) and a `cargo-tarpaulin` Test Coverage job, and bumped the GitHub Actions toolchain (checkout, cache, build-push, buildx) to clear the June Node 20 deprecation.

## By service

### proof service — helmcharts
- New chart on `finternet-base`: deployment/configmap/service/serviceaccount/ingress + helpers, 165-line values aligned to runtime config, `V1_20` `proof_status` polling migration, `database_url`/TLS docs, and initial changelog. (#133)

### proof service — units-automation (GitOps / ArgoCD)
- ArgoCD app registered at wave 3, dev namespace added, composite `database_url` GSM secret wired, network policy extended, dev image tag set. (#133)

### wpbe-silencelabs (CI / build)
- Migrated CI + Docker build from decommissioned Shipyard.rs to the Kellnr sparse index (token-based, no SSH); restored `git` in the builder stage for `build.rs` version stamping; aligned RC image-tag handling with the org pattern; added Security Audit + Test Coverage jobs and bumped Actions versions. (#18)

---
*Generated from git commits by /weekly-report.*
